{% extends "base.main.html.twig" %}

{% block titre %}{% endblock %}

{% block main %}

	<div id="pricing" class="pricing-tables">
		<div class="tables-left-dec">
			<img src="{{ asset("asset/images/tables-left-dec.png") }}" alt="">
		</div>
		<div class="tables-right-dec">
			<img src="{{ asset("asset/images/tables-right-dec.png") }}" alt="">
		</div>
		<div class="container">
			<div class="row">
				<div class="col-lg-6 offset-lg-3">
					<div class="section-heading">
						<h2>Select a suitable
							<em>plan</em>
							for your next
							<span>projects</span>
						</h2>
						<span>Our Plans</span>
					</div>
				</div>
			</div>
			<div class="row">
				{% for formation in formations %}

					<div class="col-lg-6">
						<div class="item first-item">
							<h4>THEME:
								{{ formation.titre }}</h4><br>
							<div class="img">
								<img class="card-img-top" src="{{ asset("images/"~formation.image) }}" alt="">
							</div>
							<div class="card-body">
								<h5>Description</h5><br>
								<p class="card-text">
									{{ formation.description|slice(0, 90) }}
									and make up the bulk of the card's content.
								</p>
							</div>
							<ul class="list-group list-group-flush">
								<li class="list-group-item">Nombre de place:
									{{ formation.nombrePlace }}
								</li>
								<li class="list-group-item">Debut de formation:
									{{ formation.dateDebutFormation|date("m/d/Y") }}
								</li>
								<li class="list-group-item">Fin de formation:
									{{ formation.dateFinFormation|date("m/d/Y") }}
								</li>
								<li class="list-group-item">Date de publication:
									{{ formation.datePublication|date("m/d/Y") }}
								</li>
							</ul>
							<div>
								<a href="#" class="main-blue-button-hover btn btn-danger">Detail</a>
								<a href="#" class="main-red-button-hover btn btn-primary">Postuler</a>
							</div>
						</div>
					</div>

				{% endfor %}
			</div>

		</div>
	</div>

{% endblock %}
{# {% for formation in formations %}
		<div class="col-6">
			<div class="card">
				<div class="card-hearder">
					{{formation.titre}}
				</div>
				<div class="body">
					<div class="img">
						<img src="{{ asset("images/"~formation.image)}}" alt="">
					</div>
					<p>Description:{{formation.description|slice(0, 100)}}</p>
					<h2>Nombre de place:
						{{formation.nombrePlace}}</h2>
					<h3>Date de debut:
						{{formation.dateDebutFormation|date("m-d-Y")}}</h3>
					<h3>Date de fin:
						{{formation.dateFinFormation|date("m-d-Y")}}</h3>
				</div>
				<small class="text-muted">Date publication:
					{{formation.datePublication|date("m-d-Y")}}</small>
			</div>
		</div>
	{% endfor %} #}
